{
  "name": "nuxt_issue",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt ./web",
    "build": "nuxt build ./web",
    "start": "nuxt start ./web",
    "generate": "nuxt generate ./web",
    "dev-server": "node server/index.js",
    "nodemon-server": "nodemon server/index.js",
    "start-server": "NODE_ENV=production nuxt build ./web && NODE_ENV=production node server/index.js"
  },
  "dependencies": {
    "@nuxt/content": "^1.9.0",
    "core-js": "^3.6.5",
    "express": "^4.17.1",
    "nuxt": "^2.14.6"
  },
  "devDependencies": {
    "nodemon": "^2.0.6"
  }
}
